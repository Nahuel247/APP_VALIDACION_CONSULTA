# Modelos locales

La carpeta `municipio_question_validator/` contiene el modelo exportado con Hugging Face (`save_pretrained`).

Se mantiene fuera de Git mediante `.gitignore` para no subir pesos grandes al repositorio.

Si quieres usar otra ubicacion, define la variable de entorno `MODEL_DIR` antes de ejecutar Streamlit.
